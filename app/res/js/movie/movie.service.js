'use strict';
/*
var movieServices = angular.module('movieServices', ['ngResource']);

movieAppServices.factory('Movie', ['$resource',
  function($resource) {
    return $resource('movies/:movieId.json', {}, {
      query: {
        method: 'GET',
        params: {
          movieId: 'movies'
        },
        isArray: true
      }
    });
  }
]);
*/
